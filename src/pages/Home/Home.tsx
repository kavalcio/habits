import { Container } from '@radix-ui/themes';

import { AbacusAnimation } from '@/components';

export const Home = () => {
  return (
    <Container size="3">
      <AbacusAnimation />
    </Container>
  );
};
